
<%= form_with(url: '/index', method: 'get', local: true ) do |f| %>
  <%= label_tag(:postcode, "Please enter a postcode:") %>
  <%= text_field_tag(:postcode) %>
  <%= submit_tag("Search") %>
<% end %>
